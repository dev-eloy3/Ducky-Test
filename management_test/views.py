from django.shortcuts import render, get_object_or_404, redirect
from django.conf import settings
import json
import os


# def test_conocimientos_view(request):
#     # Ruta al archivo JSON
#     ruta_json = os.path.join(settings.BASE_DIR, 'management_test', 'json', 'test.json') 

#     # Leer el archivo
#     with open(ruta_json, 'r', encoding='utf-8') as archivo:
#         test_data = json.load(archivo)

#     # Enviar el contenido al template
#     return render(request, 'test_conocimientos.html', {'test': test_data})

# eloy







